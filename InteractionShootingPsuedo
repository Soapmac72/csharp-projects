/* Need two rays, one for interactions and one for shooting



Ray interactRay;
Ray shootingRay;

if interactRay hits object tagged item
lookingAtItem = true;

if lookingAtItem = true;
print "Press 'E' to pick-up";
if E is pressed
destroy item;
add item to inventory;
